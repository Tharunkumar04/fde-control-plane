name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  # Frontend CI - React + TypeScript + Vite
  frontend:
    name: Frontend (Lint + TypeCheck + Build)
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run TypeScript type checking
        run: npm run typecheck

      - name: Build frontend
        run: npm run build

  # Docker build - only if Dockerfile exists
  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    # Only run if Dockerfile exists
    if: hashFiles('Dockerfile') != ''
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: fde-control-plane:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Backend CI - Python + FastAPI (placeholder for future implementation)
  # Uncomment when backend code is added
  # backend:
  #   name: Backend (Lint + TypeCheck + Test)
  #   runs-on: ubuntu-latest
  #   
  #   services:
  #     postgres:
  #       image: postgres:16
  #       env:
  #         POSTGRES_USER: fde
  #         POSTGRES_PASSWORD: fde
  #         POSTGRES_DB: fde_control_plane
  #       ports:
  #         - 5432:5432
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #     
  #     redis:
  #       image: redis:7
  #       ports:
  #         - 6379:6379
  #       options: >-
  #         --health-cmd "redis-cli ping"
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #   
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12'
  #         cache: 'pip'
  #
  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements.txt
  #
  #     - name: Run Ruff linter
  #       run: ruff check .
  #
  #     - name: Run MyPy type checking
  #       run: mypy .
  #
  #     - name: Run pytest
  #       run: pytest tests/ -v
  #       env:
  #         DATABASE_URL: postgresql://fde:fde@localhost:5432/fde_control_plane
  #         REDIS_URL: redis://localhost:6379
  #         MODEL_PROVIDER: mock
